{

  system.defaults.CustomUserPreferences = {
    "com.apple.symbolichotkeys" = {
      AppleSymbolicHotKeys = {
        "60" = { enabled = false; }; # ⌘ + Space
        "61" = { enabled = true; }; # ⌃ + opt + Space -> Select previous input source
        "64" = { enabled = false; }; # Spotlight
        "65" = { enabled = true; }; # ⌃ + opt + Space -> Select next input source

        # Screenshots
        "28" = { enabled = false; }; # Save picture of screen as a file
        "29" = { enabled = false; }; # Copy picture of screen to the clipboard
        "30" = { enabled = false; }; # Save picture of selected area as a file
        "31" = { enabled = false; }; # Copy picture of selected area to the clipboard
        "184" = { enabled = false; }; # Screenshot and recording options
      };
    };
  };
}
